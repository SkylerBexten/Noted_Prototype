
import string
alphabet = list(string.ascii_lowercase)
for letter in alphabet:
	print str(letter.upper()) + str(letter)
	
import string
alphabet = list(string.ascii_lowercase)
for letter in alphabet:
	print str(letter.upper()) + str(letter)
	
import string
alphabet = list(string.ascii_lowercase)
for letter in alphabet:
	print str(letter.upper()) + str(letter)
	
import string
alphabet = list(string.ascii_lowercase)
for letter in alphabet:
	print str(letter.upper()) + str(letter)